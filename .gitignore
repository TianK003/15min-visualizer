# Node / Next.js
node_modules/
.next/
out/
dist/
.pnpm-store/
.npm/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.python-version
.pytest_cache/
.mypy_cache/

# Env files (never commit)
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Build outputs
*.log
coverage/

# Raw data & ETL intermediates — too large for git; regenerate via DATA_SOURCES.md + backend/etl
data/15min-slo/*.pbf
data/15min-slo/*.gpkg
data/15min-slo/*.jsonl
data/15min-slo/cell_scores.json
data/15min-slo/tiles/

# TypeScript build cache
**/*.tsbuildinfo
# (keep the smaller geojsons checked in for now; remove these lines to ignore them too)
# data/15min-slo/*.geojson

# Local Valhalla tile builds
backend/valhalla/tiles/
backend/valhalla/custom_files/
